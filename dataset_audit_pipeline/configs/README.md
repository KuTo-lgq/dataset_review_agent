# Config Templates

This directory is intentionally left empty in the open-source release.

Create your own dataset config here, for example:

- `configs/your_dataset.json`

You can start by copying:

- `../config.example.json`
